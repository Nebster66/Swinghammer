extends AnimatedSprite2D

func _ready() -> void:
	#var animations = sprite_frames.get_animation_names()  # Get all animation names
	#if animations.size() > 0:
		#var random_animation = animations[randi() % animations.size()]  # Pick a random animation
		#play(random_animation)  # Play the selected animation
	pass
